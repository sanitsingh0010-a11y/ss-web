[build]
  publish = "public"
  command = "mkdir -p public && cp index.html public/ && echo 'Build complete'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  port = 8888
  targetPort = 8080
